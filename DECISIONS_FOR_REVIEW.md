# DECISIONS_FOR_REVIEW.md — 待负责人复核的决策

> Phase 0 自主推进期间所有"我自己拍板"的决策。验收时逐条核对，尤其手感/尺寸类。

## [工程] 仓库结构整理
- 情况：开工时文档全在仓库根目录，`cs16-revival-scaffold.zip` 里是带 `docs/` 的脚手架结构。
- 我选的默认值：按 CLAUDE.md 的目录约定把 6 份文档移入 `docs/`，`.gitignore` 取自脚手架 zip。zip 本身被 `.gitignore`（`*.zip`）忽略，留在原地未删。
- 负责人需确认：zip 是否可删。

## [工程] Godot 版本与位置
- 情况：机器上未安装 Godot。
- 我选的默认值：Godot **4.6.3-stable**（官方最新稳定版），下载在 `F:\Project\tools\godot\`（仓库外，不入库）。
- 负责人需确认：版本是否锁定 4.6.3。

## [手感] 碰撞体用 Box 而非胶囊
- 情况：CLAUDE.md 提过"胶囊体"，BUILD_PLAN 只写"碰撞体（32×32×72）"。
- 我选的默认值：`BoxShape3D` 32×32×72 —— GoldSrc 玩家 hull 就是 AABB 盒。站箱子边沿、卡点、贴墙判定只有盒型才对。
- 负责人需确认：试玩时重点感受站边沿（箱子边、pit 边）是否像 1.6。

## [手感] 视角参数
- FOV：垂直 73.74°（= 4:3 下水平 90°，即 1.6 默认视野）。16:9 下水平约 100°。如要"严格 90 水平"可改 `Camera3D.keep_aspect` 方案。
- 鼠标：0.022°/count（GoldSrc m_yaw/m_pitch 默认）× `sensitivity`（默认 2.0，`@export` 可调），pitch 限 ±89°。
- 负责人需确认：FOV 方案、灵敏度换算是否跟你 1.6 的肌肉记忆一致。

## [手感] 重力积分方式
- 情况：GoldSrc 实际用"半步重力"（半程在移动前加、半程在移动后加），spec 伪代码是整步 Euler（移动前一次加满）。
- 我选的默认值：严格照 spec 伪代码（整步）。副作用：100 tick 下实际跳跃峰值比解析值 45 略高约 1%。
- 负责人需确认：若跳跃微感"飘/高"，下一步改半步重力（改动很小，已留注释定位）。

## [手感] bhop 为"按住跳自动连跳"
- 情况：MOVEMENT_SPEC §5 写明"落地瞬间如果跳跃键仍按住 → 立即重新起跳"。但 1.6 原版 +jump 需要松开重按（所以玩家绑滚轮）。
- 我选的默认值：照 spec 实现自动连跳（按住空格即可连跳）。
- 负责人需确认：要不要还原"必须重新触发跳键"（届时滚轮绑跳）。这是手感核心差异点。

## [手感] 贴地吸附距离用 18（step 高度）
- 情况：GoldSrc 落位归类 trace 是 2 单位；但纯 2 单位在下坡/下台阶时每 tick 会误判离地（导致下坡丢摩擦、手感发飘）。
- 我选的默认值：地面判定 trace 2 单位（照 GoldSrc），但移动后的贴地吸附用 18（step 高度），仅当 velocity.y ≤ 0 且落点可站立时吸附。
- 负责人需确认：走下坡/下台阶时是否有"被吸住"感；跑下台阶想起跳是否被粘住。位置：`player_controller.gd` `_walk_move()` 末尾。

## [手感] 连跳无速度上限（默认）
- spec 已列为待决策项：`bhop_speed_cap_enabled` 默认 false（经典无限连跳）。上限逻辑本身未实现（开关已预留），等负责人决定要不要还原后期 1.6 行为再做。

## [地图] 分区共用世界坐标
- 所有分区场景直接用统一世界坐标建模，总装时全部放原点。改某分区尺寸时注意接口坐标（各区根节点 editor_description 里写了范围）。

## [地图] 总体坐标网格（校准基准表）
| 区域 | x 范围 | z 范围 | 备注 |
|---|---|---|---|
| T 出生 | -384..384 | 944..1456 | 768×512 |
| 中路 | -112..112 | -788..944 | 宽 224，长 1732；双门 z=-500；Xbox z=700 |
| CT 出生 | -384..384 | -1300..-788 | 768×512 |
| 长A入口通道 | 384..950 | 1000..1200 | Long Doors x=660 |
| 长A主直道 | 950..1206 | -580..1200 | 宽 256 长 1780 |
| Pit | 820..950 | -580..-350 | 地面 -96 |
| A短道/猫道 | 112..688 | -580..-172 | 猫道面高 64 |
| A 点 | 560..1340 | -1220..-580 | 780×640 |
| CT→A 通道 | 384..560 | -1196..-1004 | 平地无坡 |
| B 隧道上层 | -1100..-384 | 1000..1192 | 地面 0 |
| B 隧道下层 | -1100..-908 | -580..400 | 地面 -64，两端台阶 |
| B 点 | -1340..-700 | -1220..-580 | 640×640，Plat 高 128 |
| 拱门通道 | -700..-384 | -1196..-1004 | 洞宽 160 高 128 |

## [地图] Xbox 箱高 60（原版约 64）
- 情况：64 高需要蹲跳（跳 45 + step 18 = 最高 63），Phase 0 没有蹲。
- 我选的默认值：60，保证"一跳上箱"测试项可执行。
- 负责人需确认：加入蹲跳后是否恢复 64。A 点箱堆同理做成 48→64→96→128 渐进（48 起步可直接跳上，后面逐级 +16/+32）。

## [地图] B Plat 上法
- 情况：Plat 128 高不可直接跳上（原版靠车/箱借力）。
- 我选的默认值：Plat 南侧加 7 级 16 高台阶（可跑上）。
- 负责人需确认：要不要改成"借箱跳"路线还原。

## [地图] Pit 细节
- Pit 深 96，内置 48 高逃生箱（否则跳不出来）；Pit 与 A 点之间是实墙不连通（原版可从 pit 上 long/site 边沿）。待校准。

## [地图] 简化项汇总（灰盒 v1 范围取舍）
- B 隧道只有 2 个拐弯 + 一组上下层台阶（原版更多段、更绕）。
- CT→A 是平直通道（原版有坡道上 site）。
- mid 北端直接开进 CT 出生区（原版 CT mid 有结构）；mid→B doors 未做，B 只能走隧道或拱门。
- 拱门洞宽 160（文档"宽约 192"按通道宽理解，留了 16 门垛）。
- 双门/Long Doors 只是固定门洞，无门扇（照文档）。
- 全图墙高统一 192，无屋顶、无天窗结构。

---

# ★ dust2 精确几何：净室 BSP 提取（2026-06-13，负责人授权读 bsp）

## 授权与红线
- 负责人明确授权读 `de_dust2.bsp` 的**几何坐标**（不受版权保护的空间布局）。
- `tools/bsp_extract.gd` **只读** brush 顶点/边/面/平面；纹理仅读「名字」过滤 sky/trigger/clip；**绝不**提取贴图像素/模型/音效/实体布置/光照。
- 源 bsp 经 `.gitignore` 永不入库（连同 *.dem/*.wad/*.spr）。仓库里只有**我们生成的灰盒坐标**（`assets/dust2/world_geo.mesh` + `world_col.res`）。这与"几何=事实不受版权"的立场一致（CS2 也是净室重制）。

## 成果（这是目前最准的方案，取代之前所有估算/demo 重刻）
- 整张 dust2 的精确几何，**坐标与 1.6 完全一致**：9932 三角，包围盒 X[-2560,1920]×高[-192,416]×Z[-3776,1536]。
- 验证：对 400 个 demo 真实走点射线下探，**92.3% 精确吻合脚高(±18)**；A 点精确命中 96；顶视与 demo 点云完全重合。
- 走查：打开 `scenes/test_bsp.tscn` 按 **F6**——这是真 dust2 几何，带你的 demo 点云对照。

## 已知待补（剩余 ~8%）
- 箱子/平台/栏杆这类 **brush 实体**：部分带 `origin` 偏移（偏移值在 entities lump 里），我目前没读 entities（那是另一类数据），所以这些实体几何可能缺失或错位——表现为个别箱子踩不到/落到下层。占比小，下一步处理（读 dmodel/origin 或在授权范围内补 entities 的 origin 字段）。
- 动态门按编译位置纳入（灰盒可接受）。
- 可视网格个别面可能因绕序单面剔除（碰撞不受影响；走查若见"透明墙"告诉我，我给 bsp 网格开双面材质）。

## 后续路线（待负责人确认方向）
- 用这套精确几何**替换主地图 v2/v3 的全部估算分区**（dust2.tscn 改用 dust2_world）。
- 之前的 a_site_v3 / v2 估算分区可作废。手感测试场景（surf/靶场）保留。
- 出生点、门、可破坏物等若要还原，需读 entities——届时再请你授权那一类。

---

# A 区按 demo 实测重刻（a_site_v3，已被 BSP 精确几何取代）

## 怎么验收
- 在 Godot 里打开 `scenes/test_a_site.tscn` 按 **F6** 单独运行（不影响主地图）。
- 场景里有**彩色点云 = 你 demo 实际走过的轨迹**（蓝=低/红=高）。灰盒几何应当贴着点云。出生在长 A 进场口朝北看 site。
- 走一遍：长A进场坡上 site、跳那个 64 高箱（顶160）、上后平台（128）、走东侧上坡、下西南的坑。对着记忆看哪里还差。

## 实测数据来源（全部来自你的 amap.dem，非估算）
`tools/demo_heightmap.gd` 把轨迹按 32 网格取「该格最高踩点」——你跳上箱子/楼梯时记录的就是那个面的高度。A 区读出（GoldSrc 原生脚高）：
- site 主地面 **96**；后方平台 **128**（高出地面 32）；
- 跳箱顶 **160**（footprint x1056-1168 / y2392-2456，坐在96地面=**64高箱**）；
- 东侧坡 **30→143** 单调爬升（连 long 高度到平台层）；
- 长A进场 **0** + 0→96 过渡坡；西南坑底 **-24**；东侧 60 ledge；
- 边界墙：北 y2880 / 东 x1792 / 西 x865（你贴墙跑出来的）。
- 10/10 落点测试在这些高度精确命中（`tests/test_a_site.gd`）。

## 待定/下一步
- a_site_v3 用 **GoldSrc 原生坐标**，与主地图的 v2 估算坐标不在同一系。**确认 A 区形状对了之后，我按同一条 demo 把其余区（长A/中路/B/隧道/出生）逐区重刻迁移到原生坐标，整图替换 v2。**
- demo 里没跳的箱（网格上是"洞"）我按邻近高度补了 2 个中箱，位置待你指认。
- 坑的精确边界、东 60 ledge 的真实用途（sniper/rafter？）待你走查确认。

---

# demo 实测管线（2026-06-12 晚，负责人录制 amap.dem 后）

## [测量] 管线已打通：.dem → 逐帧坐标 → 墙线/楼层实测
- 工具：`tools/demo_extract.gd`（HLDEMO 解析，只取坐标/视角数字事实）+ `demo_analyze.gd`（全图俯视/楼层聚类）+ `demo_region.gd`（子区墙线峰值，贴墙距离 16 反推墙坐标）。
- 负责人 demo：de_dust2 全图贴墙跑，55728 帧。全图实测框 X[-1825,1792] Y[-1008,3072]，主楼层（脚高）-4/28/60/92/124，低区 -132/-116。
- **结论：整张图将以 demo 原生坐标系重刻（v3），v2 的估算坐标全部作废待替换。**

## [A 区实测（GoldSrc 原生坐标，墙=贴墙峰值±16）]
- 长A东墙：**x≈1792**（全程，南到北）；长A南段西墙 ≈1280；地面：门口段 0，向北渐升 8→32。
- 坑带（y 1850..2160）：坑区墙 x≈1280/1312、y≈1888..1920（南）与 2080..2112（北）；**坑底 ≈0，长A临坑段 ≈32-56，A 点 96 —— 坑比 site 低 ~96、比长A低 ~32-56**（之前 v2 猜的 -32/-224 不对）。
- A site：主地面 **96**，高台/Plat 层 **128**（112/160 为箱顶），北墙 y≈2790、南界 y≈2280-2312、东墙 1792 延续、西界 x≈992-1150 阶梯状（短道口/CT 楼梯口）。
- site 内部箱体/平台边在 x 1184/1248/1276/1328、y 2337/2432/2465/2560 一组峰上，足够摆出真实箱位。
- 下一步：按这些实测值重刻 A 区（native 坐标），其余区域从同一条轨迹逐区提取重刻；区间临时接口错位在全部迁移完成前属预期。

---

# v2 真实比例重排（2026-06-12，回应"差很远"反馈）

## [地图] v2 坐标基准（校准底表）
世界框取自社区公开的 dust2 雷达参数（pos_x=-2400, pos_y=3383, scale=4.4 → 全图 4506²，与 1.6 同源布局）。Godot 坐标：x=东，-z=北。各区（x 范围 / z 范围 / 地面高）：
| 区域 | x | z | h |
|---|---|---|---|
| T 家 | -1100..-200 | 300..1000 | 160 |
| T 大坡 | -560..-260 | 60..300 | 160→0 |
| 中路 | -560..-260 | -1750..60 | 0 |
| 中门→CT 中路 | -560..-260 | -2250..-1750 | 0 |
| 出生口外 | -200..620 | -520..560 | 160→96 |
| A 大门 | x=648 | 洞 -440..-180 | 96 |
| 长A拐角 | 676..1700 | -520..-100 | 96→32 |
| 长A直道 | 1410..1700 | -1800..-100 | 32 |
| A 大坡 | 1410..1700 | -2100..-1800 | 32→192 |
| 大坑 | 1150..1410 | -2150..-1850 | -32 |
| A 点 | 950..1700 | -2800..-2100 | 192（A 平台 224） |
| 猫道→A短 | -260..950 | -2400..-820 | 0→64→96→160→192 |
| CT 家 | 100..800 | -2700..-2050 | 64 |
| CT 楼梯 | 800..950 | -2700..-2500 | 64→192 |
| B 门走廊 | -1100..-560 | -2200..-2000 | 0→64 |
| B 点 | -1980..-1100 | -2750..-1980 | 64（Plat 192） |
| 隧道西腿 | -1850..-1100 | 420..680 | 160→96 |
| 隧道北腿 | -1810..-1590 | -1980..420 | 96 |
| 下隧道 | -1590..-560 | -700..-510 | 0（连中路！） |

- 负责人逐区报偏差（"长A 应再长 200""中路宽 260"），我按表改。
- 真实拓扑补全：下隧道连中路、B 门走廊、CT 中路、出生口外；Xbox 在猫道口旁；T 家→中路无墙开放大坡。
- 已知近似：中路在原版略带斜角（这里取正交）；猫道的 cat→CT 中路跳下沿未做；B 点窗位未做；长A拐角房体未做内部。

---

# 标高与结构 pass（2026-06-12，按你"B 方案：完整结构还原"指示）

## [地图] 全图标高剖面（核心校准对象）
- 我定的剖面：**T 家 128 / 中路·CT·B 点·拱门 0 / 长A直道 64 / A 点 160 / 猫道顶 224 / 隧道上层 64·下层 0 / Pit -32**。
- 坡道：中路大坡 20.4°（z 600..944）、长A入口坡 13°、长A大坡 21.8°、猫道东腿 11.3°+北腿 18.9°、隧道 T 坡 11.5°。全部 < 45° 可正常行走。
- 负责人需确认：每段相对高差和坡长是"距离感"的本体，按记忆逐段报偏差我来调。
- 猫道跳下 A 点落差定为 64（224→160）；原版跳下会掉血的那个落差如果记忆里更大，告诉我数。

## [地图] 结构物取舍
- B 隧道封顶（上层净高 128、下层 128）+ 两处洞口门头——隧道内 SDFGI 自然变暗，氛围接近原版。
- 中门/长A门加顶盖、拱门通道封顶；CT→A 做成 10 级楼梯（每级 16 高，可平滑跑）。
- **顺带修了一个老 bug**：CT→A 通道从 Phase 0 起只有两侧墙没有地板（走过去会坠落重生），无人发现因为测试没走过这条路。已补。
- B 点加 48 高车体掩体（用混凝土材质占位，原版是车，美术阶段可换模型）。
- 仍未做：长A拐角房体内部、B 点窗户位、CT 家顶棚——这些需要更多记忆细节，等你校准时口述我来补。

## [测试] 标高断言基准
- 出生贴地断言改为 y≈128；隧道路点 64/0；新增走坡贴地（空中 tick=0）与上坡到 160 两条。共 91 断言。

---

# 几何细化 + 合法美术决策（2026-06-12，按更新后的 MAP_DUST2/ART_DUST2）

## [流程] 美术先于几何锁定（按你的指示）
- ART_DUST2 写的顺序是"几何锁定（dust2-geometry-v1）后再贴美术"，但你这次指示"全部做好再验收"，所以美术已上。
- 风险已控制：纹理全在 4 个共享材质 `.tres` 里，几何校准改尺寸**不需要返工美术**（triplanar 无 UV 依赖）。校准锁定后打 tag 即可。

## [几何] 本轮还原清单落地情况与取值
- 中路宽窄：top mid 288 / 双门段 224（分界 z=400）。具体宽度差待你校准。
- 双门、Long Doors 门楣：洞高统一 128（同拱门）。1.6 实际门洞高度凭记忆调。
- A 默认架点：双层箱 60+120（地面→60→120 逐级可跳）。
- B 隧道 S 弯：下层西移 96（肘部 z 40..160），全程 3 个 90° 弯；B 点南口随迁 x∈[-1196,-1004]。拐弯位置/段长待校准。
- B 点箱层次：48/60/96/Plat128。
- **没做的**：全图统一标高（T 家高于中路、A 点高于长 A 等真实落差）——动全图地板牵连所有接口与测试，建议你校准平面布局后单独立项做标高。出生点到各点的"距离感"数值现状未变，等你实跑判断。

## [美术] 资产选择（全部 CC0，登记在 ASSET_CREDITS.md）
- 砂岩砖墙=ambientCG Bricks084 / 沙地=Ground054 / 木箱=Planks037A / 混凝土=Concrete034（1K JPG，Color+NormalGL+Roughness）；天空=Poly Haven syferfontein_1d_clear 2K HDRI。
- 风格判断（你验收）：暖色 tint 我调在 albedo_color 上（砖墙 1/0.93/0.8 等），不满意改 4 个 .tres 即可整体换色调；贴图密度 uv1_scale：墙 128 单位一铺、地 192、箱 64、混凝土 96。
- 1K 分辨率为带宽考虑；要更细可换 2K（ambientCG 同链接 `_2K-JPG.zip`）。
- 金属门/护栏材质类型（ART 清单第 5 类）暂未引入——当前几何没有金属件，等加门框/护栏时再下载登记。

## [文档] CLAUDE.md 仓库结构里列了 VISION.md / MOVEMENT_PHASE1.md / FUTURE_PHASES.md / GLOSSARY.md，但仓库里没有这些文件（本次只收到 MAP_DUST2 更新与 ART_DUST2 新增）。如果你本地有，记得提交。

---

# Phase 2-4 决策（2026-06-12，连续自主推进）

## [范围] Phase 1 参数锁定仍未执行
- 按你的指示："参数调优和锁定留给我"。所有功能默认值已就位，满意后你来锁定 + 打 tag。

## [手感] AK-47 数值与弹道（全部待你校准）
- 伤害 36 / 衰减 0.98^(d/512) / 爆头 ×4 / 弹匣 30+90 / 换弹 2.45s 取 CS 1.6 原值。
- **射速取 0.1s/发**（10 tick 整对齐）而非 1.6 的 0.0955s——固定步长下整 tick 最稳。嫌慢 4.5% 可改 0.09。
- **30 发弹道模式是近似形状**（前 4 发小、7-8 发爬到 ~8°、后段左右摆 ±2.5°），在 `resources/weapons/ak47.tres` 的 `spray_pattern` 数组逐发可调——这是你最需要上手校准的部分。
- 扩散：站 0.08°/蹲 ×0.6/移动 1.6°/空中 3°/移动阈值 140，全部近似可调。
- 视角 punch 仅视觉（每发 +0.9° 指数回落）；弹道用 Head 真实瞄准角——压枪压 pattern。若你记忆中"punch 也影响弹着"，告诉我改。

## [手感] 命中区域
- 靶子：身体盒 + 头盒两区。联机玩家：单盒 hull，命中点高于脚上 56 算头（近似）。正式 hitbox（头/胸/腿分区+倍率）待后续。

## [画质] Phase 3 范围与性能
- 用的全是引擎能力：SDFGI（按英寸世界调 cell=6）+ SSAO + ACES + 轻雾 + 程序噪声 triplanar 材质。**没有引入任何外部资产**——真 PBR 贴图/模型按约定等你提供。
- **SDFGI 较重**：若帧率不行，WorldEnvironment → Environment → SDFGI 取消勾选即可，不影响其他。
- 程序噪声材质只是可读性升级，灰度区分约定保持（地面中灰/墙浅灰/箱棕/抬高蓝灰）。

## [联机] 架构决策
- **服务器权威 + 客户端预测/回滚**：预测与权威跑同一 `simulate()`，局域网实测回滚 0 次、误差 0.000——1.6 手感在联机下不变形的根基。
- 服务器输入队列空时**等待而非外推**（外推会破坏 seq 对齐导致无谓回滚，已踩坑修复）。
- 快照 50Hz、远端插值延迟 100ms（`SNAPSHOT_INTERVAL`/`INTERP_DELAY` 可调）。1.6 默认 interp 也是 100ms（ex_interp 0.1）。
- 射击：客户端本地出弹（音画零延迟）+ 射线上报服务器权威结算。**没有 lag compensation（回溯命中）**——对手感影响：高 ping 下要提前量。这是 Phase 4 最大的留白，做的话需要服务器保存玩家位置历史按射手延迟回溯。记为下一步候选。
- 防作弊仅做了最低限度（射线起点贴头校验）。按 ROADMAP，反作弊原型期不碰。
- 联机蹲：hull 同步走快照，远端蹲/站只影响显示与服务器命中体；正式 hitbox 时再细化。

## [工程] 其他
- 地图拆分：`dust2_map.tscn`（本体）+ `dust2.tscn`（单机主场景）+ `dust2_mp.tscn`（联机）。
- 专服：`godot --headless res://scenes/dust2_mp.tscn -- --server [端口]`；客户端自动连：`-- --connect ip[:端口]`。
- CLAUDE.md「当前阶段」已更新为 Phase 0-4 完成等验收（原文写 Phase 0，已过时）。
- 占位枪声是代码生成的白噪声爆发（零资产）；正式音效等你提供。

---

# Phase 1 决策（2026-06-12）

## [手感] 蹲参数全部取 HL SDK 常量
- 我选的默认值：`duck_speed_multiplier=0.333`（PLAYER_DUCKING_MULTIPLIER）、`time_to_duck=0.4s`（TIME_TO_DUCK）、hull 72→36（VEC_DUCK_HULL）、眼高 64→30（VEC_DUCK_VIEW 12 + 半高 18）。
- 负责人需确认：**蹲眼高 30** 是否符合记忆（有人觉得 1.6 蹲视角更高，但 SDK 常量就是 12+18）；蹲下/站起过渡是否跟手。

## [手感] 空中蹲瞬时 + 脚抬 18（duck-jump）
- GoldSrc 空中收 hull 时 origin 不动（origin 居中）→ 等效脚抬 18。我们 origin 在脚，实现为 `global_position.y += 18`。实测 duck-jump 脚部顶点 61.7（普通跳 ~45）。
- 空中站起 = 脚放回 18（带空间检测，不行就原地站起，再不行保持蹲）。GoldSrc PM_UnDuck 只做一次 trace 失败即作罢；我加了"原地站起"兜底，行为是超集。
- 负责人需确认：duck-jump 上 64 箱的时机窗口手感。

## [手感] 蹲速倍率在过渡期不生效
- 0.333 限速只在**完全蹲下**（hull 已切换）后生效，过渡 0.4s 内仍全速——对应 GoldSrc FL_DUCKING 才触发 PM_CheckParamters 限速。1.6 的"duck 滑步"小技巧依赖这个时序。

## [手感] 台阶视角平滑 150 u/s
- HL V_CalcRefdef 的 stair smoothing：上/下台阶时镜头偏移按 150 u/s 线性回归，上限 18。`step_smooth_speed` 可调。
- 负责人需确认：连续跑楼梯（隧道/猫道/B plat）镜头是"丝滑"还是"拖泥带水"，150 不对就调。

## [手感] surf 验证结论（无代码改动）
- Phase 0 的 clip_velocity + 法线 0.7 阈值 + 空中无摩擦组合**天然产生 surf**：自由滑 0.6s 到 415.7；压坡可骑面爬升（空气加速沿面上坡 ~15/tick > 重力沿坡 ~7/tick @100tick）。
- 测试图 `scenes/test_surf.tscn`（60° 单面坡）。**只有一条坡**，要练正反交替/转体需要双面 spine，待你试完决定要不要加。
- 已知小毛病：落上斜面的**第一帧**可能误判 1 tick 地面（穿透恢复法线），骑面期间为 0。若试玩感觉"沾一下"，方案是改用 ray 取法线，先记着不动。

## [地图] Xbox 箱仍是 60，未恢复 64
- 现在有 duck-jump 了（可上 64），但恢复 64 属于地图校准，按约定留给你拍板（A 点箱堆里已有 64 箱可先试 duck-jump）。

## [范围] 未实现项（明确不做，等你指令）
- +speed 走路键（Shift walk）——ROADMAP Phase 1 没列。
- 连跳速度上限逻辑——开关 `bhop_speed_cap_enabled` 仍是占位，等你定。
- `movement_params.tres` 锁定——按你的要求留给你试玩定夺后执行（锁定时打 tag）。

## [测试] 集成测试里的两处实测值备注
- 跳跃初速断言放宽到 >245：读数时机晚 2 tick，已被重力扣掉 16（268.3 − 16 ≈ 252）。公式本身在单元测试里精确验证（≈268.33）。
- air strafe 实测：320 → 362（50 tick 匀速转向）。负责人手感验证以实际试玩为准。
